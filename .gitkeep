# Assets Folder

This folder is for optional assets like icons, images, or other resources.

The application currently uses emoji icons in the HTML, but you can replace them with image files if desired.




